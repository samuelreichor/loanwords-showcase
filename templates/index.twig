<!DOCTYPE html>
<html lang="en-US">
<head>
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta charset="utf-8" />
    <title>Welcome to Craft CMS</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no, viewport-fit=cover" />
    <meta name="referrer" content="origin-when-cross-origin" />
    <style>
        html,
        body {
            font-size: 16px;
            -webkit-text-size-adjust: 100%;
            height: 100%;
            font-family: system-ui, BlinkMacSystemFont, -apple-system, 'Segoe UI', 'Roboto', 'Oxygen', 'Ubuntu', 'Cantarell', 'Fira Sans', 'Droid Sans', 'Helvetica Neue', sans-serif;
        }

        body {
            margin: 0;
            padding: 0;
            background-color: hsl(212, 60%, 97%);
            color: hsl(209, 18%, 30%);
            display: flex;
        }

        h1 {
            margin-top: 0;
        }

        h2 {
            margin-top: 24px;
            font-size: 1em;
        }

        h2:first-child {
            margin-top: 0;
        }

        p {
            line-height: 1.4em;
            margin-bottom: 1.4em;
        }

        ul {
            line-height: 1.3em;
            padding-left: 20px;
            margin-bottom: 0;
        }

        ul li {
            margin-bottom: 0.35em;
        }

        a {
            color: #0B69A3;
            text-decoration: none;
        }

        a:hover {
            text-decoration: underline;
        }

        small {
            color: hsl(211, 11%, 59%);
        }

        code {
            display: inline-block;
            color: #EF4E4E;
            padding: 0 2px;
            background: hsl(212, 60%, 97%);
            border-radius: 3px;
            line-height: 1.3;
            font-family: "SFMono-Regular", Consolas, "Liberation Mono", Menlo, Courier, monospace;
            font-size: 0.9em;
        }

        .container {
            margin-left: auto;
            margin-right: auto;
            max-width: 1000px;
            min-height: 100vh;
            display: flex;
            align-items: center;
        }

        .modal {
            text-align: center;
        }

        .nav-item-wrapper {
            display: flex;
            gap: 2rem;
        }
    </style>
</head>
<body class="ltr">
<nav>
    <ul class="nav-item-wrapper">
        <li>
            <a href="/">
                EN
            </a>
        </li>
        <li>
            <a href="/de">
                DE
            </a>
        </li>
        <li>
            <a href="/fr">
                FR
            </a>
        </li>
        <li>
            <a href="/it">
                IT
            </a>
        </li>
    </ul>
</nav>
<div class="container">
    <div class="modal">
        <h1>{{ entry.title }}</h1>
        <p>{{ a11yTextReplacer(entry.plainTextTranslatable) }}</p>

        {{ a11yTextReplacer(entry.richtext) }}
    </div>
</div>
</body>
</html>
